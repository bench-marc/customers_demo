<!DOCTYPE html>
<html>
  <head>
    <title>CustomersDemo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  </head>

  <body>
    <!-- Top Bar -->
    <div class="bg-blue-500 p-4 text-white">
      <div class="container mx-auto flex justify-between items-center">
        <a href="#" class="text-xl font-semibold">Kundendatenbank</a>

        <!-- Navigation Links -->
        <nav>
          <ul class="flex space-x-4">
            <li><a href="<%= new_customer_path %>" class="hover:underline">New Customer</a></li>
            <li><a href="<%= customers_path %>" class="hover:underline">Customers</a></li>
          </ul>
        </nav>
      </div>
    </div>

    <!-- Page Content -->
    <div class="container mx-auto mt-8 p-6 bg-white rounded-md shadow-md">
      <% flash.each do |key, value| %>
        <div class="bg-<%= key == 'notice' ? 'blue' : 'red' %>-100 border border-<%= key == 'notice' ? 'blue' : 'red' %>-400 text-<%= key == 'notice' ? 'blue' : 'red' %>-700 px-4 py-2 rounded-md mb-4" role="<%= key %>">
          <%= value %>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
